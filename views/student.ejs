<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Đăng nhập Học Sinh</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <header class="app-header">
    <a class="brand" href="/">NudgeMath</a>
  </header>
  <div class="container">
  <div class="card">
    <h1>Đăng nhập Học Sinh</h1>
    <% if (error) { %>
      <div class="alert alert-danger"><%= error %></div>
    <% } %>
    <form method="POST" action="/student/login" class="form" style="grid-template-columns: 1fr 1fr auto;">
      <input class="input" type="text" name="username" placeholder="Username" required />
      <input class="input" type="password" name="password" placeholder="Mật khẩu" required />
      <button class="btn btn-secondary" type="submit">Đăng nhập</button>
    </form>
    <a class="footer-link" href="/">Quay lại Trang chủ</a>
  </div>
  </div>
</body>
</html>

