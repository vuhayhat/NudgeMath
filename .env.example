PORT=3000
SESSION_SECRET=thay_doi_chuoi_bi_mat

# Một trong hai cách cấu hình DB:
# 1) Sử dụng URL kết nối
# DATABASE_URL=postgres://user:password@host:5432/dbname

# 2) Hoặc tách biến
# PGHOST=localhost
# PGPORT=5432
# PGDATABASE=nodemath
# PGUSER=postgres
# PGPASSWORD=postgres

# Bật SSL nếu dùng dịch vụ cloud có yêu cầu
# SSL_MODE=require

# Seed tài khoản admin mặc định (tùy chọn)
DEFAULT_ADMIN_EMAIL=admin@example.com
DEFAULT_ADMIN_PASSWORD=changeme

# Seed tài khoản học sinh mặc định (tùy chọn)
DEFAULT_STUDENT_USERNAME=student01
DEFAULT_STUDENT_PASSWORD=changeme

# Bảo mật và AI
JWT_SECRET=change-me
UPLOAD_DIR=uploads
GEMINI_API_KEY=your-key-here
GEMINI_MODEL=gemini-2.5-flash
